# 🔍 HRMS 后端需求完成情况全面检查报告

> **检查日期**: 2026-07-16
> **检查范围**: 需求文档 v1.0 全部后端需求（第2-11节）
> **Java 源文件**: 211 个
> **单元测试**: 298 通过，0 失败，0 错误

---

## 📊 总体概况

| 指标 | 结果 |
|------|------|
| **编译状态** | ✅ JDK 17 下编译通过（JDK 25/26 与 Lombok 不兼容，需用 JDK 17） |
| **单元测试** | ✅ **298 测试通过，0 失败，0 错误** |
| **需求覆盖率** | 约 **85%**（核心业务逻辑基本完整，存在可修复的缺口） |

---

## 一、权限体系（第2节）— 完成度：85%

| 需求 | 状态 | 说明 |
|------|:----:|------|
| 5种角色定义 | ✅ | `SysRole` + SQL种子数据完整定义系统管理员/HR专员/部门主管/财务专员/普通员工 |
| 数据权限规则 (2.2表) | ✅ | `DataScopeAspect` + `DataScopeHelper` + `@RequirePermission` 完整实现5种数据范围 |
| **字段级权限 (2.3表)** | ⚠️ | `EmployeeService.toVO()` 未做运行时角色过滤，敏感字段（身份证/薪资/银行卡）对所有角色都返回；`ResignationService.getMaskedDetail()` 标注 `// TODO` |

### 详细说明

**已实现：**
- `SysRole` entity + `phase2-rbac.sql` 种子数据完整插入5种角色（`ROLE_ADMIN`, `ROLE_HR`, `ROLE_MANAGER`, `ROLE_FINANCE`, `ROLE_EMPLOYEE`）
- `DataScopeEnum` 定义5种数据范围枚举：`ALL_PLATFORM(1)`, `ALL_EMPLOYEE(2)`, `DEPT_AND_SUB(3)`, `SALARY_ONLY(4)`, `SELF_ONLY(5)`
- `DataScopeAspect` 切面拦截 `@DataScope` 注解，自动注入SQL过滤条件
- `PermissionAspect` 拦截 `@RequirePermission` 注解，系统管理员自动跳过所有权限检查
- `DataScopeHelper` 根据不同 dataScope 生成对应的 SQL WHERE 子句

**待修复：**
- `EmployeeService.toVO()` 中所有敏感字段无条件填充，未根据 `BaseContext.getDataScope()` 做角色过滤
- `ResignationService.getMaskedDetail()` 脱敏方法为 TODO 存根

---

## 二、组织架构管理（第3节）— 完成度：70%

| 需求 | 状态 | 说明 |
|------|:----:|------|
| 部门 CRUD | ✅ | 完整增删改查 + 权限控制（`org:dept:view` / `org:dept:manage`） |
| 部门字段 | ⚠️ | **缺少 `description`（描述）字段**，其他5个字段（名称/编码/上级/负责人/排序）均有 |
| 层级限制5级 | ✅ | `DepartmentService` 中 `MAX_DEPT_LEVEL = 5` 硬编码校验 |
| 部门人数统计 | ⚠️ | 仅统计直属在职员工（试用期+正式），**未递归汇总子部门人数到父部门** |
| 部门合并+员工转移 | ❌ | **完全未实现**，无 merge 方法或接口 |
| 职位 CRUD | ✅ | 完整增删改查 + 按序列(M/P/S)筛选 |
| 职位字段 | ⚠️ | **缺少 `所属部门`（deptId）字段**，其他5个字段均有 |
| 职级对照种子数据 | ⚠️ | M序列5/5完整；P序列仅P1-P7（缺P8-P10）；S序列仅S1-S3（缺S4-S5） |

---

## 三、员工档案管理（第4节）— 完成度：85%

| 需求 | 状态 | 说明 |
|------|:----:|------|
| 基础信息/个人信息/工作信息 | ✅ | `Employee` entity 包含所有需求字段（共20个字段） |
| 薪资与合同信息 | ✅ | `Employee` + `EmployeeContract` 完整覆盖合同类型/到期日/试用期比例/薪资账套/银行信息 |
| **高级搜索** | ⚠️ | 仅支持关键词(姓名+工号) + 单个部门 + 单个状态筛选；**缺少以下5项** |
| — 手机号模糊搜索 | ❌ | phone 已加密不支持 LIKE，需通过 `phone_hash` 精确匹配替代 |
| — 部门树多选 | ❌ | 仅支持单个 `deptId` |
| — 职位多选 | ❌ | 无 positionId 筛选参数 |
| — 职级多选 | ❌ | 无 grade 筛选参数 |
| — 入职日期范围 | ❌ | 无 startDate/endDate 参数 |
| 工号生成 | ✅ | `EmployeeNoGenerator` 格式 `年份(4)+部门编码(2)+序号(3)`，含并发冲突重试（最多3次） |
| 敏感字段加密 | ✅ | 身份证/手机号/银行卡/工资/地址等8个字段通过 `EncryptedStringTypeHandler` / `EncryptedBigDecimalTypeHandler` 加密，超出需求范围 |

---

## 四、入转调离流程（第5节）— 完成度：90%

| 需求 | 状态 | 说明 |
|------|:----:|------|
| **入职全流程** | ✅ | 5阶段状态流转（草稿→审批中→已批准待入职/已拒绝→已入职） |
| — 审批流 | ✅ | 部门负责人 → [HR负责人](条件可选)，基于职位标准性+薪资范围动态判定 |
| — 自动处理 | ✅ | 审批通过后自动生成工号、创建系统账号、建立员工档案、发送通知 |
| — 撤回 | ✅ | HR可撤回（仅限第一级且无审批人已处理） |
| **转正全流程** | ✅ | 3分支通过/延长试用/辞退 + 2级审批(部门→HR) |
| — 到期提醒 | ✅ | `LifecycleScheduler` 每天08:00扫描试用期到期前7天员工，通知主管+HR |
| **调岗全流程** | ✅ | 状态校验(试用期/正式) + 部门必变 + 3级审批(原部门→新部门→HR) + 条件财务审批 |
| — 生效处理 | ✅ | 更新档案 + 记录 `EmployeeTransfer` 历史 + 工号不变 + 通知员工 |
| **离职全流程** | ⚠️ | 主体流程完整，但4项生效处理缺失 |
| — 状态变更+账号禁用 | ✅ | `LifecycleScheduler` 每天00:30处理待离职→已离职 |
| — **工号标记释放** | ❌ | 代码中无任何工号释放/回收逻辑 |
| — **从考勤组移除** | ❌ | `LifecycleScheduler.processPendingResignations()` 无考勤组移除操作 |
| — **薪资计算至离职日期** | ❌ | 未实现基于 resignDate 的最终薪资结算 |
| — 保留脱敏档案 | ⚠️ | `getMaskedDetail()` 方法框架存在但脱敏逻辑为 `// TODO` |

---

## 五、考勤管理（第6节）— 完成度：70%

| 需求 | 状态 | 说明 |
|------|:----:|------|
| 考勤组配置 | ⚠️ | 已实现：名称/班次类型(固定班+弹性班)/上下班时间/弹性范围 |
| — 适用人员 | ❌ | 实体中无 `deptId`/`positionId`/`employeeIds` 关联字段 |
| — 排班制 | ❌ | `groupType` 仅有固定班(1)+弹性班(2)，无排班制 |
| — 中午休息 | ❌ | 无午餐/休息时段字段 |
| — 迟到/早退阈值 | ❌ | 无独立阈值字段，复用 `flexThreshold` + `absentHalfDayThreshold` 组合判定 |
| 工作日设置 | ✅ | 周一至周五默认工作日 + `WorkCalendar` 法定节假日/调班配置 + `batchUpsert` 批量导入 |
| 打卡功能 | ✅ | punch-in/punch-out 双接口 + 完整状态判定（正常/迟到/早退/旷工半天/缺卡） |
| 缺卡处理 | ✅ | `MISSING_PUNCH` 标记 + 上下班分别判定 |
| 补卡申请 | ⚠️ | 申请/审批/回写考勤/防重复均已实现；**缺少每月最多2次限制** |
| 请假管理 | ✅ | 7种类型(年假/调休/事假/病假/婚假/产假/丧假) + 天数控(`LeaveDayCalculator`) + 条件必填附件 |
| 请假审批流 | ✅ | 完全按 6.3.4表规则实现（年假/调休≤3天→上级；>3天→上级+部门；事假/病假≤1天→上级；>1天→上级+部门；婚假/产假/丧假→上级+HR备案） |
| 年假计算 | ⚠️ | 年假档位正确（<1年=0, 1-10年=5, 10-20年=10, ≥20年=15）；**缺少当年入职按剩余月份折算** |
| 调休管理 | ✅ | 加班1:1转换(8h=1天) + 当月及次月有效 + FIFO扣减 + 过期清零任务 + 拒绝/取消回补 |
| 考勤统计 | ⚠️ | 个人统计缺：应出勤天数/实际出勤天数/迟到次数/旷工天数/加班时长；部门统计缺：出勤率/迟到率 |
| AntV 可视化 VO | ✅ | `LeaveTypeDistributionVO` / `DeptLeaveRateVO` / `PersonalLeaveStatsVO` / `LeaveStatsByTypeVO` 4个VO齐全 |

---

## 六、薪资管理（第7节）— 完成度：80%

| 需求 | 状态 | 说明 |
|------|:----:|------|
| 薪资账套 CRUD | ✅ | `SalaryPlan` + `SalaryPlanItem`(6种category) + `SalaryPlanScope`(按部门/职位/职级) |
| 工资项目类型 | ✅ | itemType(INCOME/DEDUCTION) × category(FIXED/VARIABLE/ATTENDANCE/SOCIAL/FUND/TAX) 完整组合 |
| 员工薪资档案 | ✅ | `SalaryAccount` 含：适用账套/基本工资/岗位津贴/绩效基数/社保基数/公积金基数 |
| 试用期薪资 | ✅ | `probationRatio` 仅影响基本工资+岗位工资，社保公积金按全额基数缴纳 |
| 调薪历史记录 | ✅ | `SalaryChangeHistory` 完整审计追踪（CREATE/ADJUST/DEACTIVATE） |
| 个税累计预扣法 | ✅ | `SalaryCalculationService.calcCumulativeTax()` 标准7档税率表 + 累计计算 |
| **核算流程** | ⚠️ | 主体流程完整，但缺少**考勤数据锁定**步骤；Entity定义的 CALCULATING/PENDING_CONFIRM 状态未使用 |
| **异常检测** | ⚠️ | 3项预警已实现（请假>15天黄色/加班>50h黄色/薪资变动>30%红色）；**红色阻断检测**（新员工无薪资档案）未实现 |
| 工资条权限 | ✅ | `PayslipService` 强制仅本人 + 审批通过(APPROVED/PAID/ARCHIVED)后才可见 |
| **工资条二次验证** | ⚠️ | 框架到位（PASSWORD/SMS枚举），但密码验证标注 `// TODO: 验证密码`；**无SMS实现** |
| 工资条内容 | ✅ | `SalaryRecord` 含收入明细/扣除明细/实发金额完整字段 |

---

## 七、审批中心（第8节）— 完成度：✅ 100%

| 需求 | 状态 | 说明 |
|------|:----:|------|
| 7种审批类型 | ✅ | `BusinessTypeEnum`: 入职/转正/调岗/离职/薪资批次/请假/补卡 |
| 待办列表 | ✅ | `ApprovalTodoVO` 含申请人/类型/申请时间/截止时间 + `ApprovalWorkbenchService.getTodoList()` |
| 审批详情页 | ✅ | `ApprovalDetailVO` 含完整业务数据 + 审批历史（含代审留痕） + 当前用户可操作项 |
| 通过/拒绝/退回 | ✅ | `ApprovalActionEnum`: APPROVE(1)/REJECT(2)/RETURN(3) |
| 转交 | ✅ | `ApprovalWorkbenchService.transfer()` 含本人校验+目标用户校验+通知 |
| 委托审批-设置 | ✅ | `ApprovalDelegationService.create()` 含重叠时间段检测 |
| 委托审批-自动改派 | ✅ | `ApprovalStateMachineService.applyDelegationIfActive()` 生成审批记录时自动改派 |
| 委托审批-取消 | ✅ | `ApprovalDelegationService.cancel()` 仅委托人本人可取消 |
| 委托审批-留痕 | ✅ | `buildAgentComment()` 对转交/委托自动附加"XXX代YYY审批"前缀 |

---

## 八、个人中心（第9节）— 完成度：65%

| 需求 | 状态 | 说明 |
|------|:----:|------|
| 我的档案 | ⚠️ | **无专门的"我的档案"端点**（所有端点需显式传 employeeId，未使用 `BaseContext.getCurrentEmployeeId()`）；无可编辑/锁定字段区分逻辑 |
| 我的考勤-打卡 | ✅ | punch-in/punch-out 已实现 |
| 我的考勤-请假 | ✅ | `LeaveController.apply()` / `submit()` 已实现 |
| 我的考勤-补卡 | ✅ | `SupplementaryCardController.apply()` / `my()` 已实现 |
| 我的考勤-日历视图 | ❌ | **无日历视图数据接口**（无按日汇总考勤状态的端点） |
| 我的请假 | ✅ | 申请列表 + 审批进度查看 + 取消申请(审批中+未被处理) |
| 我的薪资-列表 | ✅ | `SalaryController.myPayslips()` 已实现 |
| 我的薪资-详情 | ⚠️ | 二次验证密码校验为 TODO 存根 |
| 我的薪资-趋势 | ⚠️ | 趋势接口为全局聚合而非个人维度 |
| 账号安全 | ✅ | 修改密码含旧密码验证 + 新密码强度校验 + BCrypt编码 |

---

## 九、非功能需求（第11节）— 完成度：60%

| 需求 | 状态 | 说明 |
|------|:----:|------|
| 密码强度规则 | ✅ | `PasswordUtil.isStrong()`: `^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$` 8位+大小写+数字 |
| **90天强制更换密码** | ❌ | `SysUser.pwdUpdateTime` 字段存在但**登录/定时任务均无校验逻辑** |
| 敏感数据加密 | ✅ | AES-256-GCM + EncryptedString/BigDecimal TypeHandler + SHA-256哈希索引 + pepper加盐 + HKDF密钥派生 |
| 操作日志审计 | ⚠️ | 工资条查看已审计(`PayslipViewLog`)；**无系统级 AuditLog**；**无批量导出功能** |
| **30分钟无操作登出** | ❌ | JWT TTL = `86400000`ms = **24小时**，无 inactivity 超时机制 |

---

## 十、数据安全（CLAUDE.md）— 完成度：✅ 100%

| 需求 | 状态 | 说明 |
|------|:----:|------|
| AES-256-GCM 加密 | ✅ | `EncryptionUtil`: `AES/GCM/NoPadding`，128-bit认证标签，12字节随机IV |
| MyBatis TypeHandler | ✅ | `EncryptedStringTypeHandler` + `EncryptedBigDecimalTypeHandler`，写入自动加密、读取自动解密 |
| SHA-256 哈希辅助索引 | ✅ | `phone_hash`/`id_card_hash`/`bank_account_hash`，格式 `SHA-256(plaintext + pepper)` |
| 主密钥环境变量注入 | ✅ | `@Value("${HRMS_ENCRYPTION_KEY:}")`，非空则抛 `IllegalStateException` |
| HKDF 派生 DEK/HMAC Key | ✅ | RFC 5869 Section 2.3，派生 DEK + HMAC Key + Pepper 三个独立密钥 |
| 加密版本管理 | ✅ | `encryption_version` 字段预留，`DataEncryptionMigrator` 迁移工具已就位 |

---

## 🔴 需要优先修复的关键问题（共13项）

### 高优先级（安全相关）

| # | 问题 | 所属模块 | 影响 |
|---|------|----------|------|
| 1 | **EmployeeService.toVO() 未实现字段级权限过滤** | 权限体系 | 部门主管可见员工身份证号/薪资/银行卡号，违反2.3表 |
| 2 | **工资条二次验证密码校验为 TODO 存根** | 薪资管理 | 员工首次查看工资条时密码验证形同虚设 |
| 3 | **90天强制更换密码未实现** | 安全 | 违反11.2安全要求 |
| 4 | **30分钟无操作自动登出未实现** | 安全 | JWT TTL=24h，违反11.2安全要求 |

### 中优先级（功能完整）

| # | 问题 | 所属模块 | 影响 |
|---|------|----------|------|
| 5 | 离职生效缺少：工号标记释放 | 离职 | 需求5.4.4 |
| 6 | 离职生效缺少：从考勤组移除 | 离职 | 需求5.4.4 |
| 7 | 离职生效缺少：薪资计算至离职日期 | 离职 | 需求5.4.4 |
| 8 | 高级搜索缺少4种多选+日期范围 | 员工档案 | 需求4.2.2 |
| 9 | 薪资核算红色阻断检测未实现（新员工无档案） | 薪资管理 | 需求7.3.3 |
| 10 | 个人中心缺少日历视图端点 | 个人中心 | 需求9.2 |

### 低优先级（完善增强）

| # | 问题 | 所属模块 | 影响 |
|---|------|----------|------|
| 11 | 部门缺少 description 字段 | 组织架构 | 需求3.1.2 |
| 12 | 职位缺少所属部门字段 | 组织架构 | 需求3.2.1 |
| 13 | 部门人数未递归汇总子部门 | 组织架构 | 需求3.1.4 |

---

## ✅ 做得特别好的地方

1. **审批中心**：实现完整度 100%，审批状态机 + 条件跳过 + 委托审批 + 转交全部实现，架构设计优秀
2. **数据安全**：AES-256-GCM + TypeHandler透明加密 + SHA-256哈希索引 + HKDF密钥派生，实现质量超出需求文档要求
3. **入转调离**：入职/转正/调岗三个模块几乎 100% 覆盖，状态流转严谨，审批流灵活可配置
4. **请假系统**：7种类型 + 复杂审批流规则（6.3.4表精确实现） + FIFO调休扣减 + 余额自动过期，业务逻辑细致
5. **测试覆盖**：298个测试全部通过，核心业务逻辑有良好的测试保障
6. **异常处理**：统一的 `BaseException` + `GlobalExceptionHandler`，错误信息规范

---

## 📋 模块完成度汇总

| 模块 | 需求节 | 完成度 | 评价 |
|------|:------:|:------:|------|
| 权限体系 | §2 | 85% | 基础设施完善，字段级过滤待补 |
| 组织架构 | §3 | 70% | CRUD完整，细节字段和统计待完善 |
| 员工档案 | §4 | 85% | 核心功能完整，高级搜索待扩展 |
| 入转调离 | §5 | 90% | 核心流程完整，离职后续处理待补 |
| 考勤管理 | §6 | 70% | 打卡请假完整，考勤组和统计待完善 |
| 薪资管理 | §7 | 80% | 计算引擎完善，流程状态待细化 |
| 审批中心 | §8 | **100%** | 全部实现，架构优秀 |
| 个人中心 | §9 | 65% | 基础功能有，专用端点待补 |
| 非功能需求 | §11 | 60% | 加密超预期，安全配置待加强 |
| 数据安全 | CLAUDE.md | **100%** | 加密体系完整 |
| **总体** | | **~85%** | 核心业务完整，需安全加固 |

---

> **检查总结**：后端核心业务逻辑已完成约 **85%**，所有主要流程都能跑通。剩余主要是安全加固（字段权限过滤、密码过期、会话超时）和一些边界功能的完善（高级搜索、离职后续处理、个人中心专用端点）。整体代码质量高，架构设计合理，测试覆盖充分。
>
> **编译注意**：项目 target Java 17，当前系统 JDK 25/26 存在 Lombok 兼容性问题，需使用 JDK 17 编译：
> ```bash
> JAVA_HOME=/Users/Zhuanz/Library/Java/JavaVirtualMachines/ms-17.0.16/Contents/Home mvn compile
> ```
