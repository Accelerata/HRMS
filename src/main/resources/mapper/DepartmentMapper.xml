<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hrms.mapper.DepartmentMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.hrms.entity.Department">
        <id column="id" property="id"/>
        <result column="parent_id" property="parentId"/>
        <result column="dept_name" property="deptName"/>
        <result column="dept_code" property="deptCode"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="level" property="level"/>
        <result column="status" property="status"/>
        <result column="manager_id" property="managerId"/>
        <result column="description" property="description"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 查询所有部门 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM department
        WHERE status = 1
        ORDER BY sort_order ASC, id ASC
    </select>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM department WHERE id = #{id}
    </select>

    <!-- 根据父ID查询子部门 -->
    <select id="selectByParentId" resultMap="BaseResultMap">
        SELECT * FROM department
        WHERE parent_id = #{parentId} AND status = 1
        ORDER BY sort_order ASC, id ASC
    </select>

    <!-- 根据层级查询 -->
    <select id="selectByLevel" resultMap="BaseResultMap">
        SELECT * FROM department WHERE level = #{level}
    </select>

    <!-- 统计子部门数 -->
    <select id="countByParentId" resultType="int">
        SELECT COUNT(*) FROM department WHERE parent_id = #{parentId}
    </select>

    <!-- 插入 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO department (parent_id, dept_name, dept_code, sort_order, level, status, manager_id, description, create_time, update_time)
        VALUES (#{parentId}, #{deptName}, #{deptCode}, #{sortOrder}, #{level}, #{status}, #{managerId}, #{description}, NOW(), NOW())
    </insert>

    <!-- 更新 -->
    <update id="update">
        UPDATE department
        SET parent_id    = #{parentId},
            dept_name    = #{deptName},
            dept_code    = #{deptCode},
            sort_order   = #{sortOrder},
            level        = #{level},
            status       = #{status},
            manager_id   = #{managerId},
            description  = #{description},
            update_time  = NOW()
        WHERE id = #{id}
    </update>

    <!-- 删除 -->
    <delete id="deleteById">
        DELETE FROM department WHERE id = #{id}
    </delete>

    <!-- 编码唯一性 -->
    <select id="countByCode" resultType="int">
        SELECT COUNT(*) FROM department
        WHERE dept_code = #{deptCode}
        <if test="excludeId != null">AND id != #{excludeId}</if>
    </select>

    <!-- 同层级名称唯一性 -->
    <select id="countByNameAndParent" resultType="int">
        SELECT COUNT(*) FROM department
        WHERE dept_name = #{deptName} AND parent_id = #{parentId}
        <if test="excludeId != null">AND id != #{excludeId}</if>
    </select>

    <!-- 递归查询子部门ID列表（CTE递归） -->
    <select id="selectSubDeptIdsRecursive" resultType="java.lang.Long">
        WITH RECURSIVE dept_tree AS (
            SELECT id, parent_id FROM department WHERE id = #{deptId}
            UNION ALL
            SELECT d.id, d.parent_id FROM department d
            INNER JOIN dept_tree dt ON d.parent_id = dt.id
        )
        SELECT id FROM dept_tree WHERE id != #{deptId}
    </select>

</mapper>
